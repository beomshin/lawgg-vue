class DeleteCommentTrialRequestDto {
    id
    password

    constructor(
        id,
        password
    ) {
        this.id = id;
        this.password = password
    }

}

export default DeleteCommentTrialRequestDto
